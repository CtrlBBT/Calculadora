function adicao(numero1, numero2) {
    if (numero1 != Number | numero2 != Number) {
        let numero1 = prompt("Digite números válidos. Digite o Primeiro Número para a Conta: ");
        let numero2 = prompt("Digite o Segundo Número para a Conta: ");
    } else {
        let soma = numero1 + numero2        

        return soma
    }
}

module.exports = soma;